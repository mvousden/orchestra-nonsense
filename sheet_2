    populate_note(&note, G3,  CROTCHET, CROTCHET_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    /* */
    populate_note(&note, G3,  CROTCHET, CROTCHET_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    /* */
    populate_note(&note, G3,  CROTCHET, CROTCHET_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    /* */
    populate_note(&note, G3,  CROTCHET, CROTCHET_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER_T, QUAVER_T_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, G3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    populate_note(&note, GB3,  QUAVER, QUAVER_BREATH);
    send(ssocket, (void*)&note, sizeof(note), 0);
    /* end */
    populate_note(&note, -1, 0, -1);
    send(ssocket, (void*)&note, sizeof(note), 0);
